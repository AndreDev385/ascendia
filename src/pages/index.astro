---
import RootLayout from "../layouts/RootLayout.astro";
import { Icon } from "astro-icon/components";
---

<RootLayout>
  <header>
    <div class="flex justify-center py-4">
      <img
        class="mb-4 max-h-10 lg:mb-0 lg:max-h-16"
        src="../../public/logo_emerald.png"
        alt="logo"
      />
    </div>
  </header>
  {/* Hero section */}
  <section
    class="relative flex min-h-screen bg-gradient-to-br from-slate-50 via-white to-emerald-50 px-4 lg:py-8"
  >
    {/* Background Elements */}
    <div
      class="bg-grid-slate-100 absolute inset-0 -z-10 [mask-image:linear-gradient(0deg,white,rgba(255,255,255,0.6))]"
    >
    </div>
    <div
      class="absolute top-0 right-0 h-96 w-96 animate-pulse rounded-full bg-emerald-100 opacity-70 mix-blend-multiply blur-xl filter"
    >
    </div>
    <div
      class="absolute bottom-0 left-0 h-96 w-96 animate-pulse rounded-full bg-blue-100 opacity-70 mix-blend-multiply blur-xl filter delay-1000"
    >
    </div>

    <div class="relative z-10 mx-auto w-full max-w-7xl">
      <div class="grid items-center gap-12 lg:grid-cols-2">
        {/* Contenido Principal */}
        <div class="space-y-8">
          {/* Headline Principal */}
          <div class="space-y-6">
            <h1
              class="text-4xl leading-tight font-bold text-gray-900 lg:text-5xl xl:text-6xl"
            >
              Diseñamos estrategias que {" "}
              <span
                class="bg-gradient-to-r from-emerald-600 to-blue-600 bg-clip-text text-transparent"
              >
                impulsan tus ventas
              </span>{" "}
            </h1>
            <p class="max-w-lg text-xl leading-relaxed text-gray-600">
              Con los tres pilares fundamentales: <strong>Publicidad</strong>, <strong
                >Email Marketing</strong
              > y un{" "}
              <strong>Sistema de ventas</strong>. Te garantizamos conectar con
              tu cliente ideal, aumentar tus ventas e impulsar tu marca.
            </p>
          </div>

          {/* Beneficios Clave */}
          <div class="space-y-3">
            {
              [
                "Empieza tu estrategia personalizada",
                "Conecta con tu cliente ideal",
                "Incrementa tus ventas",
              ].map((benefit, index) => (
                <div key={index} class="flex items-center gap-3">
                  <Icon
                    name="mdi:checkbox-marked-circle-auto-outline"
                    class="text-emerald"
                  />
                  <span class="font-medium text-gray-700">{benefit}</span>
                </div>
              ))
            }
          </div>

          {/* CTAs */}
          <button
            type="button"
            class="group relative flex transform items-center justify-center gap-2 overflow-hidden rounded-xl bg-gradient-to-r from-emerald-500 to-emerald-600 px-8 py-4 font-bold text-white shadow-lg transition-all duration-300 hover:scale-105 hover:from-emerald-600 hover:to-emerald-700 hover:shadow-xl"
          >
            <span>Contáctenos</span>
            <Icon
              name="mdi:arrow-right"
              class="size-5 transition-transform group-hover:translate-x-1"
            />
            <div
              class="absolute inset-0 bg-white opacity-0 transition-opacity group-hover:opacity-10"
            >
            </div>
          </button>
        </div>

        {/* Visual y Elementos Gráficos */}
        <div class="relative space-y-8">
          {/* Dashboard Mockup */}
          <div class="relative">
            <div
              class="rounded-2xl border border-gray-100 bg-white p-6 shadow-2xl"
            >
              <div class="space-y-4">
                {/* Header del Dashboard */}
                <div class="flex items-center justify-between">
                  <h3 class="font-semibold text-gray-900">Panel de Control</h3>
                  <div class="flex gap-2">
                    <div class="h-3 w-3 rounded-full bg-red-400"></div>
                    <div class="h-3 w-3 rounded-full bg-yellow-400"></div>
                    <div class="h-3 w-3 rounded-full bg-green-400"></div>
                  </div>
                </div>

                {/* Métricas */}
                <div class="grid grid-cols-2 gap-4">
                  <div class="rounded-lg bg-emerald-50 p-4">
                    <div class="mb-2 flex items-center gap-2">
                      <Icon
                        name="mdi:target"
                        class="h-5 w-5 text-emerald-600"
                        <span
                        class="text-sm font-medium text-emerald-800"
                        >Conversiones</Icon
                      >
                    </div>
                    <div class="text-2xl font-bold text-emerald-900">+347%</div>
                  </div>
                  <div class="rounded-lg bg-blue-50 p-4">
                    <div class="mb-2 flex items-center gap-2">
                      <Icon name="mdi:email" class="h-5 w-5 text-blue-600" />
                      <span class="text-sm font-medium text-blue-800"
                        >Email ROI</span
                      >
                    </div>
                    <div class="text-2xl font-bold text-blue-900">4,200%</div>
                  </div>
                </div>

                {/* Gráfico Simulado */}
                <div class="space-y-2">
                  <div class="flex justify-between text-sm text-gray-600">
                    <span>Ventas este mes</span>
                    <span class="font-semibold text-emerald-600">↗ +89%</span>
                  </div>
                  <div class="h-2 overflow-hidden rounded-full bg-gray-200">
                    <div
                      class="h-full w-4/5 animate-pulse rounded-full bg-gradient-to-r from-emerald-500 to-blue-500"
                    >
                    </div>
                  </div>
                </div>
              </div>
            </div>

            {/* Elementos flotantes */}
            <div
              class="absolute -top-4 -right-4 animate-bounce rounded-full bg-emerald-500 p-3 text-white shadow-lg"
            >
              <Icon name="mdi:trending-up" class="size-6" />
            </div>
            <div
              class="absolute -bottom-4 -left-4 animate-bounce rounded-full bg-blue-500 p-3 text-white shadow-lg delay-500"
            >
              <Icon name="mdi:email" class="size-6" />
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  {/* What would you expect */}
  <section
    class="from-dark-gray to-forest-green relative overflow-hidden bg-gradient-to-br py-20"
  >
    {/* Background decorative elements */}
    <div
      class="bg-emerald/10 absolute top-0 left-0 h-72 w-72 rounded-full blur-3xl"
    >
    </div>
    <div
      class="bg-emerald/5 absolute right-0 bottom-0 h-96 w-96 rounded-full blur-3xl"
    >
    </div>

    <div class="relative z-10 container mx-auto px-6">
      {/* Header */}
      <div class="animate-fade-in-up mb-16 text-center">
        <h2
          class="mt-6 mb-4 text-4xl leading-tight font-bold text-white md:text-5xl"
        >
          Del Caos a las
          <span class="text-emerald block">Conversiones Reales</span>
        </h2>
        <p class="text-light-green mx-auto max-w-3xl text-xl leading-relaxed">
          Sistemas personalizados que transforman tu negocio en una máquina de
          ventas
        </p>
      </div>

      {/* Benefits Grid */}
      <div class="mb-16 grid gap-8 md:grid-cols-2 lg:grid-cols-3">
        {
          [
            {
              icon: <Icon name="mdi:users" class="size-6" />,
              title: "Leads Calificados",
              description:
                "Prospectos reales con intención de compra, no solo tráfico sin conversión",
            },
            {
              icon: <Icon name="mdi:calendar" class="size-6" />,
              title: "Agenda Llena de Oportunidades",
              description:
                "Citas programadas automáticamente con clientes potenciales de alto valor",
            },
            {
              icon: <Icon name="mdi:robot" class="size-6" />,
              title: "Bots Inteligentes 24/7",
              description:
                "Precalificación automática y respuestas instantáneas sin intervención manual",
            },
            {
              icon: <Icon name="mdi:trending-up" class="size-6" />,
              title: "CRM y Seguimiento Automatizado",
              description:
                "Sistema completo de gestión con embudos personalizados y seguimiento por WhatsApp",
            },
            {
              icon: <Icon name="mdi:shield" class="size-6" />,
              title: "Escalabilidad Sin Contratar Personal",
              description:
                "Crece tu negocio sin aumentar gastos operativos ni depender de más empleados",
            },
            {
              icon: <Icon name="mdi:checkbox-marked-circle-auto-outline" class="size-6" />,
              title: "ROI Comprobable",
              description:
                "Inversión que genera retorno real con seguimiento transparente de resultados",
            },
          ].map((benefit, index) => (
            <div
              key={index}
              class="group hover:border-emerald/50 animate-fade-in-up rounded-2xl border border-white/20 bg-white/10 p-8 shadow-lg backdrop-blur-sm transition-all duration-300 hover:-translate-y-2 hover:bg-white/15 hover:shadow-2xl"
              style={{ animationDelay: `${index * 0.1}s` }}
            >
              <div class="flex items-start space-x-4">
                <div class="flex-shrink-0">
                  <div class="bg-emerald/20 text-emerald group-hover:bg-emerald flex h-12 w-12 items-center justify-center rounded-xl transition-colors duration-300 group-hover:text-white">
                    {benefit.icon}
                  </div>
                </div>
                <div class="flex-1">
                  <h3 class="group-hover:text-emerald mb-3 text-lg font-bold text-white transition-colors duration-300">
                    {benefit.title}
                  </h3>
                  <p class="text-light-green/80 leading-relaxed">
                    {benefit.description}
                  </p>
                </div>
              </div>
            </div>
          ))
        }
      </div>

      <div
        class="bg-emerald/10 border-emerald/20 relative overflow-hidden rounded-3xl border p-6 lg:p-12 text-center backdrop-blur-sm"
      >
        <div
          class="from-emerald/5 to-emerald/10 absolute inset-0 bg-gradient-to-r"
        >
        </div>
        <div class="relative z-10">
          <h3 class="mb-4 text-3xl font-bold text-white md:text-4xl">
            ¿Listo para Dejar de Improvisar?
          </h3>
          <p class="text-light-green mx-auto max-w-2xl text-lg">
            No más campañas que no convierten. No más procesos manuales. Solo
            resultados que escalan tu negocio.
          </p>
        </div>
      </div>

      {/* Trust indicators */}
      <div class="mt-16 text-center">
        <div
          class="text-light-green/80 flex flex-wrap items-center justify-center gap-8"
        >
          <div class="flex items-center space-x-2">
            <Icon name="mdi:checkbox-marked-circle-auto-outline" class="text-emerald h-5 w-5" />
            <span class="font-medium">Sin contratos de permanencia</span>
          </div>
          <div class="flex items-center space-x-2">
            <Icon name="mdi:shield" class="text-emerald h-5 w-5" />
            <span class="font-medium">Garantía de resultados</span>
          </div>
          <div class="flex items-center space-x-2">
            <Icon name="mdi:trending-up" class="text-emerald h-5 w-5" />
            <span class="font-medium">ROI desde el primer mes</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  {/* How we deliver */}
 <section class="bg-gradient-to-b from-white to-light-green/30 py-16 md:py-24">
      <div class="container mx-auto px-6 lg:px-8">
        {/* Header */}
        <div class="text-center mb-12 md:mb-20 animate-fade-in-up">
          <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-dark-gray mb-6 leading-tight">
            Nuestro <span class="text-emerald">método probado</span> paso a paso
          </h2>
          <p class="text-lg md:text-xl text-gray-600 max-w-4xl mx-auto leading-relaxed">
            Transformamos tu proceso de ventas con un sistema que funciona las 24 horas, 
            los 7 días de la semana, generando leads calificados constantemente.
          </p>
        </div>

        {/* Steps for Mobile */}
        <div class="md:hidden space-y-8">
          {[
    {
      number: "01",
      title: "Análisis y Estrategia",
      description: "Estudiamos tu negocio, mercado y competencia para crear una estrategia de captación personalizada que genere leads de alta calidad desde el primer día.",
      icon: <Icon name="mdi:target" class="w-6 h-6" />,
      delay: "0s"
    },
    {
      number: "02", 
      title: "Creación de Campañas",
      description: "Diseñamos y lanzamos campañas de Meta Ads optimizadas para atraer tu cliente ideal, con creativos que convierten y audiencias precisas.",
      icon: <Icon name="mdi:electricity" class="w-6 h-6" />,
      delay: "0.2s"
    },
    {
      number: "03",
      title: "Automatización y CRM",
      description: "Implementamos bots inteligentes, sistemas de precalificación automática y CRM personalizado que gestiona leads las 24/7 sin intervención manual.",
      icon: <Icon name="mdi:checkbox-marked-circle-auto-outline" class="w-6 h-6 text-emerald" />,
      delay: "0.4s"
    },
    {
      number: "04",
      title: "Optimización y Escala",
      description: "Monitoreamos, ajustamos y escalamos tu sistema basado en datos reales para maximizar el ROI y garantizar crecimiento sostenible.",
      icon: <Icon name="mdi:trending-up" class="w-6 h-6" />,
      delay: "0.6s"
    }
  ].map((step, index) => (
            <div 
              key={index}
              class="bg-white rounded-2xl p-6 shadow-lg border border-emerald/10 hover:border-emerald/30 transition-all duration-300 animate-fade-in-up"
              style={{ animationDelay: step.delay }}
            >
              <div class="flex items-start space-x-4">
                <div class="flex-shrink-0">
                  <div class="w-12 h-12 bg-emerald rounded-xl flex items-center justify-center text-white font-bold text-lg">
                    {step.number}
                  </div>
                </div>
                <div class="flex-1">
                  <div class="flex items-center space-x-2 mb-3">
                    <div class="text-emerald">
                      {step.icon}
                    </div>
                    <h3 class="text-xl font-bold text-dark-gray">
                      {step.title}
                    </h3>
                  </div>
                  <p class="text-gray-600 leading-relaxed">
                    {step.description}
                  </p>
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* Steps for Desktop - Timeline */}
        <div class="hidden md:block relative">
          {/* Timeline line */}
          <div class="absolute left-1/2 transform -translate-x-1/2 w-1 h-full bg-gradient-to-b from-emerald/20 via-emerald/40 to-emerald/20 rounded-full"></div>

          <div class="space-y-16 lg:space-y-24">
            {[
    {
      number: "01",
      title: "Análisis y Estrategia",
      description: "Estudiamos tu negocio, mercado y competencia para crear una estrategia de captación personalizada que genere leads de alta calidad desde el primer día.",
      icon: <Icon name="mdi:target" class="w-6 h-6" />,
      delay: "0s"
    },
    {
      number: "02", 
      title: "Creación de Campañas",
      description: "Diseñamos y lanzamos campañas de Meta Ads optimizadas para atraer tu cliente ideal, con creativos que convierten y audiencias precisas.",
      icon: <Icon name="mdi:electricity" class="w-6 h-6" />,
      delay: "0.2s"
    },
    {
      number: "03",
      title: "Automatización y CRM",
      description: "Implementamos bots inteligentes, sistemas de precalificación automática y CRM personalizado que gestiona leads las 24/7 sin intervención manual.",
      icon: <Icon name="mdi:checkbox-marked-circle-auto-outline" class="w-6 h-6 text-emerald" />,
      delay: "0.4s"
    },
    {
      number: "04",
      title: "Optimización y Escala",
      description: "Monitoreamos, ajustamos y escalamos tu sistema basado en datos reales para maximizar el ROI y garantizar crecimiento sostenible.",
      icon: <Icon name="mdi:trending-up" class="w-6 h-6" />,
      delay: "0.6s"
    }
  ].map((step, index) => (
              <div
                key={index}
                class={`flex items-center gap-8 lg:gap-16 ${
                  index % 2 === 0 ? "lg:flex-row" : "lg:flex-row-reverse"
                } animate-fade-in-up`}
                style={{ animationDelay: step.delay }}
              >
                {/* Content */}
                <div class="flex-1">
                  <div class="bg-white rounded-2xl p-8 shadow-lg border border-emerald/10 hover:border-emerald/30 transition-all duration-300 hover:shadow-xl relative">
                    <div class="absolute -top-4 -left-4 w-16 h-16 bg-emerald rounded-full flex items-center justify-center text-white font-bold text-xl shadow-lg">
                      {step.number}
                    </div>
                    <div class="ml-8">
                      <div class="flex items-center space-x-3 mb-4">
                        <div class="text-emerald">
                          {step.icon}
                        </div>
                        <h3 class="text-2xl font-bold text-dark-gray">
                          {step.title}
                        </h3>
                      </div>
                      <p class="text-gray-600 leading-relaxed text-lg">
                        {step.description}
                      </p>
                    </div>
                  </div>
                </div>

                {/* Timeline dot */}
                <div class="hidden lg:flex w-6 h-6 bg-emerald rounded-full flex-shrink-0 shadow-lg z-10 relative">
                  <div class="w-3 h-3 bg-white rounded-full m-auto"></div>
                </div>

                {/* Visual element */}
                <div class="flex-1 hidden lg:block">
                  <div class="bg-gradient-to-br from-emerald/10 to-emerald/20 rounded-2xl p-8 h-64 flex items-center justify-center">
                    <div class="text-center">
                      <div class="w-20 h-20 bg-emerald/20 rounded-full flex items-center justify-center mb-4 mx-auto">
                        <div class="text-emerald">
                          {step.icon}
                        </div>
                      </div>
                      <p class="text-forest-green font-semibold">
                        Semana {index + 1}
                      </p>
                    </div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* Bottom CTA */}
        <div class="mt-16 md:mt-24 text-center">
          <div class="bg-gradient-to-r from-emerald/5 to-emerald/10 rounded-3xl p-8 md:p-12 border border-emerald/20">
            <h3 class="text-2xl md:text-3xl font-bold text-dark-gray mb-4">
              ¿Listo para comenzar tu transformación digital?
            </h3>
            <p class="text-gray-600 mb-8 text-lg max-w-2xl mx-auto">
              Únete a empresarios que ya están escalando sus ventas con nuestro sistema automatizado
            </p>
            <button class="bg-emerald text-white font-bold text-lg px-10 py-4 rounded-xl hover:bg-forest-green transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl">
              Empezar
            </button>
          </div>
        </div>

        {/* Trust indicators */}
        <div class="mt-12 flex flex-wrap justify-center items-center gap-6 md:gap-8 text-sm text-gray-500">
          <div class="flex items-center space-x-2">
            <Icon name="mdi:checkbox-marked-circle-auto-outline" class="w-4 h-4 text-emerald" />
            <span>Implementación</span>
          </div>
          <div class="flex items-center space-x-2">
            <Icon name="mdi:checkbox-marked-circle-auto-outline" class="w-4 h-4 text-emerald" />
            <span>Soporte 24/7</span>
          </div>
          <div class="flex items-center space-x-2">
            <Icon name="mdi:checkbox-marked-circle-auto-outline" class="w-4 h-4 text-emerald" />
            <span>Garantía de resultados</span>
          </div>
        </div>
      </div>
    </section>

  {/* Footer */}
  <footer class="bg-dark-gray py-20">
    <div class="container mx-auto px-6 lg:px-8">
      <div class="border-t border-gray-700 pt-8">
        <div class="grid gap-8 md:grid-cols-4">
          <div>
            <h3 class="text-emerald mb-4 text-2xl font-bold">Ascendia</h3>
            <p class="text-gray-400">
              Transformamos visitantes en clientes reales con tecnología de
              vanguardia y estrategias probadas.
            </p>
          </div>
          <div>
            <h4 class="mb-4 font-bold text-white">Servicios</h4>
            <ul class="space-y-2 text-gray-400">
              <li>Meta Ads</li>
              <li>CRM Automatizado</li>
              <li>Bots de IA</li>
              <li>Consultoría</li>
            </ul>
          </div>
          <div>
            <h4 class="mb-4 font-bold text-white">Recursos</h4>
            <ul class="space-y-2 text-gray-400">
              <li>Casos de Éxito</li>
              <li>Blog</li>
              <li>Webinars</li>
              <li>Guías Gratuitas</li>
            </ul>
          </div>
          <div>
            <h4 class="mb-4 font-bold text-white">Empresa</h4>
            <ul class="space-y-2 text-gray-400">
              <li>Sobre Nosotros</li>
              <li>Equipo</li>
              <li>Carreras</li>
              <li>Contacto</li>
            </ul>
          </div>
        </div>

        <div
          class="mt-12 border-t border-gray-700 pt-8 text-center text-gray-400"
        >
          <p>
            &copy; 2024 Ascendia. Todos los derechos reservados. Transformando
            negocios desde 2020.
          </p>
        </div>
      </div>
    </div>
  </footer>
</RootLayout>
